INPUT-
#include <stdio.h>
#include <math.h>

int main() {
    int n, i;
    double sum;

    printf("Enter a value for n: ");
    scanf("%d", &n);

    sum = 0;
    for (i = 1; i <= n; i++)
	 {
        sum += pow(i, i);
    }

    printf("The sum of the series 1^1 + 2^2 + 3^3 + 4^4 + ... %d^%d is %lf.\n", n, n, sum);

    return 0;
}

OUTPUT-
Enter a value for n: 3
The sum of the series 1^1 + 2^2 + 3^3 + 4^4 + ... 3^3 is 32.000000.
